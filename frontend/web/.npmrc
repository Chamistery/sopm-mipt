# Storybook 8 имеет нечёткие peer deps между core и addon пакетами.
# Без этого флага npm install ломается на чистой машине. Когда Storybook
# выровняет peer deps в минорной (>= 8.7), флаг можно убрать.
legacy-peer-deps=true
